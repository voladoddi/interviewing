"""
Re-order array entries such that element less than pivot element is on left
element equal to pivot is in the middle and 
all other entries(higher than pivot) are on right
"""
from typing import List


def dutch_flag(input_array: List[int]) -> None:
    """
    Dutch flag algorithm impl
    """
    print(input_array)
